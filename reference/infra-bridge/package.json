{
  "name": "@brepjs/infra-bridge-reference",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "check": "npm run build && npm run typecheck:tests && npm run typecheck:compare && npm run lint && npm run test:run",
    "build": "tsgo",
    "typecheck": "tsgo --noEmit",
    "typecheck:tests": "tsgo -p tsconfig.test.json --noEmit",
    "typecheck:compare": "tsgo -p tsconfig.compare.json --noEmit",
    "lint": "eslint src tests scripts",
    "precompare:model": "npm run build",
    "compare:model": "tsx --tsconfig tsconfig.compare.json scripts/compareInfraBridge.ts",
    "prereport:inventory": "npm run build",
    "report:inventory": "tsx --tsconfig tsconfig.compare.json scripts/reportInfraBridge.ts",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:run": "vitest run"
  },
  "dependencies": {
    "web-ifc": "^0.0.77"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^4.1.10"
  }
}
